---
import LocalizedHome from '~/components/LocalizedHome.astro';
import PageLayout from '~/layouts/PageLayout.astro';
import { localizedHome } from '~/i18n/home';

const metadata = {
  title: localizedHome.fr.metadata.title,
  description: localizedHome.fr.metadata.description,
  ignoreTitleTemplate: true,
};
---

<PageLayout metadata={metadata}>
  <LocalizedHome locale="fr" />
</PageLayout>
