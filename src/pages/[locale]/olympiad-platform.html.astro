---
import OlympiadPlatformPage from '../olympiad-platform.html.astro';
import { locales } from '~/i18n/config';

export function getStaticPaths() {
  return locales
    .filter((locale) => locale !== 'ar')
    .map((locale) => ({
      params: { locale },
    }));
}
---

<OlympiadPlatformPage />
